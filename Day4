// Find cube of any number using inline function!
#include <iostream>
using namespace std;
inline int cube(int n)
{
    return (n * n * n);
}
int main()
{
    int c;
    c = cube(10);
    cout << "Cube of the number: " << c;
    return 0;
}
